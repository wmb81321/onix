## Unformatted Snippets

Any snippets that are intended to live in `.mdx` files as code examples with `// [!code **]` annotations live here so the formatter doesn't try to move those annotations to a newline.
